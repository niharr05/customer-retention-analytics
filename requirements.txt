# ==========================================================================
# Customer Engagement & Retention Analytics - Dependencies
# ==========================================================================
# Install with:  pip install -r requirements.txt
#
# Note on kaleido: version 0.2.1 is pinned intentionally. Kaleido >= 1.0
# requires a separate Google Chrome installation to export static PNG
# images from Plotly figures. Version 0.2.1 ships its own embedded
# renderer and works out-of-the-box in most environments, including
# headless servers and CI pipelines.
# ==========================================================================

pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
plotly>=5.18.0
scipy>=1.10.0
streamlit>=1.30.0
kaleido==0.2.1
pytest>=7.0.0
